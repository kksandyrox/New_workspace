var win = Ti.UI.createWindow();

var webview = Ti.UI.createWebView({
	url:'webview.html',
	top:0,
	left:0,
	width:'100%',
	height:80
	});
	
var button = Ti.UI.createButton({
	
});
	
win.add(webview);
win.open();
